# Simple todo app to training

## Usage

- install the dependencies: `npm install`

- set .env variable using .env.example as base

- Run database migrations: `node ace migration:run`

- run the server with `npm run dev`
